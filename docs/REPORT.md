**Project Status**

This document captures recent actions (augmentation + OOF tooling) and exact commands so Miss Sania can review progress.

**Augmentation**:

- **Script**: `scripts/augment_and_save.py` — offline augmentation that copies originals and generates per-class augmented images until a target per-class count is reached.
- **Action taken**: Ran augmentation to produce ~350 images/class (total 2,100 train images). Original train folder backed up at `data/processed_backup/train_backup` and augmented train placed at `data/processed/train`.
- **Command used**:

```
.\.venv\Scripts\python.exe scripts\augment_and_save.py --input data/processed/train --output data/processed_aug/train --targets 350 --quality 90 --size 224
```

**OOF generation (5-fold CV)**:

- **Script**: `scripts/gen_oof_preds.py` — performs stratified 5-fold CV on `data/processed/train` and saves OOF probability arrays and per-fold checkpoints.
- **Stratification**: Implemented using `StratifiedKFold` to preserve class proportions per fold.
- **Outputs**:
  - `outputs/{model}_oof_probs.npy` — OOF probability matrix (N x C)
  - `outputs/{model}_oof_meta.csv` — CSV mapping index → file_path → true_label
  - `outputs/checkpoints/{model}_fold{fold}_best.pt` — per-fold best checkpoint
- **Phase policy**: By default the script runs **Phase‑1 only (frozen backbone)**. Phase‑2 (unfreeze last block) is optional and only runs when `--phase2` is passed. Rationale: prior experiments showed Phase‑2 frequently degraded performance and costs double runtime.
- **Recommended commands (Phase‑1 only, faster & reliable)**:

```
# VGG19
.\.venv\Scripts\python.exe scripts\gen_oof_preds.py --model vgg19 --folds 5 --phase1-epochs 8 --batch-size 16 --lr 1e-4 --weight-decay 1e-4

# DenseNet201
.\.venv\Scripts\python.exe scripts\gen_oof_preds.py --model densenet201 --folds 5 --phase1-epochs 8 --batch-size 16 --lr 1e-4 --weight-decay 1e-4

# MobileNetV2
.\.venv\Scripts\python.exe scripts\gen_oof_preds.py --model mobilenet_v2 --folds 5 --phase1-epochs 8 --batch-size 16 --lr 1e-4 --weight-decay 1e-4
```

If you later want to test Phase‑2 for a model, append `--phase2` to the same command; expect ~2× runtime.

**Next steps (recommended)**:

- Run `scripts/gen_oof_preds.py` for the three base models (commands above).
- Implement `scripts/train_gating.py` to train a small MLP on OOF probabilities (inputs: concatenated probs or stacked features). Save gating model and evaluate fused outputs on `data/processed/test`.
- Document final fused results and include tables/figures in this report.

**Notes / provenance**:

- All scripts are in the repository under `scripts/`.
- Checkpoints are saved under `outputs/checkpoints/` by default when running `gen_oof_preds.py`.

---

Generated by the assistant on request; update this file with experiment results (accuracy, confusion matrices) after OOF runs.

# Subject-to-Change Project Report

NOTE: This report is "subject to change" — results are experimental, depend on dataset splits, random seeds, hyperparameters, and further preprocessing. Use this as a concise lab notebook and decision record.

1. Project objective

---

Implement a 6-class wound classification pipeline (background, diabetic, normal-skin, pressure, surgical, venous) using the AZH dataset (extended to six classes). Build a small/medium experimental pipeline to run safe staged transfer-learning experiments, compare backbones (VGG19, DenseNet201, MobileNetV2), and select a baseline model for scaling.

2. Environment

---

- OS: Windows
- Python: 3.11 (virtualenv used: `.venv`)
- Frameworks: PyTorch + torchvision, scikit-learn, PIL, matplotlib
- Repo structure: see `docs/README.md` (top-level summary)

3. Data & preprocessing

---

- Raw AZH images located under `data/raw/azh/wound_classification-main/ROI/`.
- Processed subset created via `src/data/preprocess.py` with balanced sampling ~50 images per class.
- Processed splits: train = 262 images, val = 63 images, test ≈ 66 images (per-class ≈10–11 in val/test).
- Train transforms: resize 224×224, flips, rotations, ToTensor, ImageNet normalization.

4. Training strategy

---

- Staged transfer learning for stability:
  - Phase 1: freeze backbone, train classifier head only.
  - Phase 2: unfreeze last feature block (only), fine-tune with lower LR (Phase2 LR = Phase1 LR / 10 by default).
- Small batches (8) due to CPU-first runs.
- Checkpoints saved to `checkpoints/`.
- Figures saved to `results/figures/`.

5. Experiments and results (to-date)

---

Commands used (examples):

```powershell
# MobileNetV2
.\.venv\Scripts\python.exe scripts\train_backbone_small.py --model mobilenet_v2 --phase 1 --epochs 10 --batch-size 8 --lr 1e-4
.\.venv\Scripts\python.exe scripts\train_backbone_small.py --model mobilenet_v2 --phase 2 --epochs 10 --batch-size 8 --lr 1e-4

# DenseNet201
.\.venv\Scripts\python.exe scripts\train_backbone_small.py --model densenet201 --epochs 10 --batch-size 8 --lr 1e-4

# VGG19
.\.venv\Scripts\python.exe scripts\train_vgg_small.py --phase 1 --epochs 10 --batch-size 8 --lr 1e-4
.\.venv\Scripts\python.exe scripts\train_vgg_small.py --phase 2 --epochs 10 --batch-size 8 --lr 1e-4
```

Key numeric results (validation set; 63 samples):

- DenseNet201 (run earlier): validation accuracy ≈ 0.5873.
- MobileNetV2:
  - Phase 1 (backbone frozen): val acc ≈ 0.5556.
  - Phase 2 (last block unfrozen): val acc = 0.4762 (performance dropped after unfreezing).
  - Confusion notes: `normal-skin` strong, `diabetic`/`venous` weak recall.
- VGG19:
  - Phase 1 (backbone frozen): val acc = 0.6825 (best performer so far).
  - Class-level notes: `normal-skin` recall 1.00; `diabetic` recall 0.45; `pressure` 0.60; `surgical` 0.55.

Artifacts saved:

- `checkpoints/mobilenet_v2_small_subset.pt`
- `checkpoints/densenet201_small_subset.pt`
- `checkpoints/vgg19_small_subset.pt`
- Training plots saved under `results/figures/` (e.g., `vgg19_small_subset_training.png`).

6. Interpretation & short analysis

---

- VGG19 Phase 1 (training head only) outperforms the other two models on this subset and split.
- MobileNetV2 Phase 2 decreased performance — likely causes:
  - Too-large phase LR for the dataset size (even lr/10 may be too aggressive for MobileNetV2 last block).
  - Possible overfitting due to small dataset and limited regularization.
  - Random variability due to split and seed.
- DenseNet201 gives competitive performance but below VGG19 Phase 1 on this split.

7. Recommendations (immediate)

---

1. Choose VGG19 Phase 1 as the working baseline for the next stage of experiments.
2. For MobileNetV2 Phase 2 re-runs, try: smaller phase LR (lr/20 or lr/100), add weight decay (1e-4), and/or unfreeze a narrower set of layers (e.g., only the final depthwise conv block). Add early stopping on val loss.
3. Record and keep the best-val checkpoint (not just final) for each phase.
4. Run 3 seeds or k-fold (if time permits) to reduce variance before final model selection.
5. Investigate label noise or ambiguous examples for classes with low recall (`diabetic`, `venous`, `surgical`), and consider class-weighted loss or oversampling those classes.

8) Recommended next experiments

---

- Finish VGG19 Phase 2 run (you're running it now). If Phase 2 improves val acc, adopt it; otherwise revert to Phase 1 weights.
- Re-run MobileNetV2 Phase 2 with lr/20 and weight decay.
- Run a single 3-fold cross-validation for the three backbones (short epochs) to get a more stable ranking.
- Add a small augmentation ablation: increase random color jitter/brightness for skin-type variability.

9. Repro commands and notes

---

- Use the `.venv` Python and run commands shown earlier. Ensure `data/processed` exists (created by `src/data/preprocess.py`).
- To reproduce exact numbers, log the seed, `--lr`, `--batch-size`, and `--phase` in an experiments CSV.

10. Change log (what we did, why, and when)

---

- Created balanced sample subset (~50 images/class) to reduce class imbalance and improve comparisons.
- Implemented `train_backbone_small.py` for MobileNetV2 and DenseNet201 with `--phase` support.
- Observed model collapse with full fine-tuning of VGG19 on tiny subsets; switched to staged training (freeze head then unfreeze last block).
- Ran DenseNet201 and MobileNetV2 Phase 1/2 experiments; summarized above.
- Implemented CLI controls in `scripts/train_vgg_small.py` and safe figure saving to avoid blocking.
- Added `docs/README.md` and this `docs/REPORT.md`.

11. Files to review next

---

- `src/data/preprocess.py` — verify class balancing and transforms.
- `scripts/train_backbone_small.py` — to tune MobileNetV2 Phase 2 hyperparams.
- `scripts/train_vgg_small.py` — Phase 2 results (currently running).

12. Who should review / decisions needed

---

- Decision: adopt VGG19 Phase 1 as baseline or wait for Phase 2. If VGG19 Phase 2 improves, adopt Phase 2 weights.
- Decision: whether to allocate time to heavier cross-validation or try lightweight hyperparameter sweep (recommended: do small sweeps first).

If you'd like, I can:

- Parse the attached Word doc into the project and extract its objectives (place it into repo and I'll parse),
- Add an `experiments.csv` template and a short script that logs runs automatically,
- Run MobileNetV2 Phase 2 with a conservative LR/weight-decay sweep (e.g., lr/20, lr/50) and save results.
